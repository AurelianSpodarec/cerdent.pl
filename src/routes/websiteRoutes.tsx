export enum WebsiteRoutes {
  ROOT = "/",
  OFFER = "#offer",
  TESTIMONIALS = "#testimonials",
  PROCESS = "#process",
  FAQ = "#FAQ",
  SERVICES = "/services",
  WORK = "/projects",
  // CONTACT = "/contact",
}
